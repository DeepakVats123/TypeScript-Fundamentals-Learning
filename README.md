# TypeScript Fundamentals

# How to run this project?
  Step 1 - Clone the repo
  Step 2 - install node module
  step 3 - after every change run this command -> npx tsx
  stpe 4 - then run this command -> npm node /dist/index.js or (npm node /dist/fileName)
